
<block wx:if="{{!cetData}}">
  <view class="weui-cells weui-cells_after-title noid-button">
      <view class="weui-cell weui-cell_switch">
          <view class="weui-cell__bd">无准考证查询</view>
          <view class="weui-cell__ft">
              <switch checked="{{type === 'noid'}}" bindchange="onSwitchChange"/>
          </view>
      </view>
  </view>
  <view class="weui-cells weui-cells_after-title">
    <view class="check-username">
      {{displayUsername}}
    </view>
    <view wx:if="{{type !== 'noid'}}" class="weui-cell weui-cell_input">
        <view class="weui-cell__hd">
            <view class="weui-label">姓名：</view>
        </view>
        <view class="weui-cell__bd">
            <input class="weui-input" focus="true" placeholder="请输入输入姓名" value="{{name}}" bindinput="nameInput" />
        </view>
    </view>
    <view wx:if="{{type !== 'noid'}}" class="weui-cell weui-cell_input weui-cell_vcode">
        <view class="weui-cell__hd">
            <view class="weui-label">准考证号</view>
        </view>
        <view class="weui-cell__bd">
            <input class="weui-input" type="number" value="{{id}}" bindinput="idInput" placeholder="请输入准考证号" />
        </view>
    </view>
    <view wx:if="{{type === 'noid'}}" class="weui-cell weui-cell_input weui-cell_vcode">
        <view class="weui-cell__hd">
            <view class="weui-label">学号</view>
        </view>
        <view class="weui-cell__bd">
            <input class="weui-input" type="number" value="{{username}}" bindinput="usernameInput" placeholder="请输入学号" />
        </view>
    </view>
    <view wx:if="{{showCaptcha}}" class="weui-cell weui-cell_input weui-cell_vcode">
      <view class="weui-cell__hd">
          <view class="weui-label">验证码</view>
      </view>
      <view class="weui-cell__bd">
          <input class="weui-input" value="{{yzm}}" bindinput="captchaInput" placeholder="请输入验证码" />
      </view>
      <view class="weui-cell__ft">
          <image bindtap="changeCaptcha" class="weui-vcode-img" src="{{cetCaptchaSrc}}" style="width: 108px"></image>
      </view>
    </view>

  </view>
  <view class="info-wrapper">
    <view class="info">*注1：看不清验证码，请点击验证码切换。</view>
    <view class="info">*注2：可选择无准考证查询，无准考证查询需要输入学号</view>
  </view>
  <button wx:if="{{!showCaptcha}}" class="weui-btn" type="primary" bindtap="confirm">查询</button>
  <button wx:if="{{showCaptcha}}" class="weui-btn" type="primary" bindtap="submit">查询</button>
</block>
<block wx:else>
    <view class="wrapper" wx:if="{{cetData.id}}">
      <view class="shadow-box">
        <view class="item center">
          <view>姓名</view><view>{{cetData.name}}</view>
        </view>
        <view class="item center">
          <view>学校</view><view>{{cetData.school}}</view>
        </view>
        <view class="item center">
          <view>考试</view><view>{{cetData.type}}</view>
        </view>
        <view class="item center">
          <view>听力</view><view>{{cetData.listen}}</view>
        </view>
        <view class="item center">
          <view>阅读</view><view>{{cetData.reading}}</view>
        </view>
        <view class="item center">
          <view>写作</view><view>{{cetData.writing}}</view>
        </view>
        <view class="item center border-top {{cetData.total >= 425 ? 'pass' : 'noPass'}}">
          <view>总分</view><view>{{cetData.total}}</view>
        </view>
      </view>
    </view>
    <view wx:else class="loadingText text">
      没有查到您今年的四六级考试成绩~
    </view>
    <navigator wx:if="{{doNotRefresh}}" url="../home/home">
      <view class="common-button-wrapper">
        <view class="common-button-flex">
          <button class="common-button">返回主页</button>
        </view>
      </view>
    </navigator>
</block>
<!-- <block wx:if="{{showCaptcha}}">
  <view class="captcha-wrapper">
    <view class="title">看不清?点击图片更换验证码</view>
    <image bindtap="changeCaptcha" class="captcha-image" src="{{ cetCaptchaSrc }}"></image>
    <input class="input captcha-input" focus="{{!showCaptcha}}" value="{{yzm}}" bindinput="captchaInput" placeholder="请输入验证码" />
    <button class="button" bindtap="submit">查询</button>
  </view>
</block> -->
